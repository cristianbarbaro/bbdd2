<%= bootstrap_form_with(model: user, url: user_path, local: true) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
  
        <ul>
        <% species.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <p>
        <strong>Email:</strong>
        <%= user.email %>
    </p>

    <%= form.hidden_field :user_id, :value => user.id %>

    <%= form.check_box :admin?, label: "Administrator", :checked => (user.has_role? :admin) %>

    <div class="actions">
      <%= form.submit "Save", class: "btn btn-success" %>
    </div>
  <% end %>