<%= bootstrap_form_with(model: [farm, plot, plot_problem], local: true) do |form| %>
  <% if plot_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plot_problem.errors.count, "error") %> prohibited this plot_problem from being saved:</h2>

      <ul>
      <% plot_problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_area :comment %>

  <%= form.hidden_field :lock_version %>

  <%= form.select(:problem_id, problems_for_select, :prompt => 'Select') %>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-success" %>
  </div>
<% end %>
