<%= form_with(model: [farm, plot, plot_problem], local: true) do |form| %>
  <% if plot_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plot_problem.errors.count, "error") %> prohibited this plot_problem from being saved:</h2>

      <ul>
      <% plot_problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="field">
    <%= form.label :problem_id %>
    <%= form.text_field :problem_id %>
  </div>

  <div class="field">
    <%= form.label :plot_id %>
    <%= form.text_field :plot_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
