<h1>Editing Crop</h1>

<%= render 'form', crop: @crop, plot: @plot, farm: @farm %>

<div class="pull-right">
    <%= link_to 'Delete', [@farm, @plot, @crop], class:'btn btn-danger', method: :delete, data: { confirm: '¿Are you sure?' }, title: 'Delete' %>
    <%= link_to 'Show', [@farm, @plot, @crop], class: "btn btn-primary" %>
    <%= link_to 'Back', :back, class: "btn btn-default" %>
</div>
