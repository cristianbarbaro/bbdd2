<h1>Editing Plot</h1>

<%= render 'form', plot: @plot, farm: @farm %>

<div class="pull-right">
    <%= link_to 'Delete', [@farm, @plot], class:'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' }, title: 'Delete' %>
    <%= link_to 'Show', [@farm, @plot], class: "btn btn-primary" %>
    <%= link_to 'Back', :back, class: "btn btn-default" %>
</div>
